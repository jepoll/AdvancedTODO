import { DefaultTheme, Provider as PaperProvider } from 'react-native-paper';

export const theme = {
  ...DefaultTheme,
  colors: {
    ...DefaultTheme.colors,
    primary: '#19196f',
    background: '#19196f',
    text: '#fff',
    lightBlue: '#249ef0',
  },
};
